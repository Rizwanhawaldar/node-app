kind: Service
apiVersion: v1
metadata:
  name: nodeapp
spec:
 containers:
  - command:
    - kube-controller-manager
    - cloud-provider=aws
  selector:
    app: nodeapp
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
  type: LoadBalancer   
